module Test {
  fun test : Bool {
    !!!!!true && !!!!false && !false
  }
}
--------------------------------------------------------------------------------
module Test {
  fun test : Bool {
    !true && !!false && !false
  }
}
