module Test {
  test : Bool {
    !!!!!true && !!!!false && !false
  }
}
--------------------------------------------------------------------------------
module Test {
  test : Bool {
    !true && !!false && !false
  }
}
