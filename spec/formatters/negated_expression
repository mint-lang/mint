module Test {
  fun test : Bool {
    !!!!!"fun" && !!!![1] && !1
  }
}
--------------------------------------------------------------------------------
module Test {
  fun test : Bool {
    !"fun" && !![1] && !1
  }
}
