module A {
  test : Promise(Never, String) {
    sequence {
      a =
        "hello"
    }finally{/* HEAD */"blah"/* TAIL */}
  }
}
--------------------------------------------------------------------------------
module A {
  test : Promise(Never, String) {
    sequence {
      a =
        "hello"
    } finally {
      /* HEAD */
      "blah"

      /* TAIL */
    }
  }
}
