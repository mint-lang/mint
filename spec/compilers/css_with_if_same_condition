component Main {
  style test {
    .tag {
      if (true) {
        color: red;
      } else {
        color: blue;
      }
    }

    .string {
      if (true) {
        color: yellow;
      } else {
        color: cyan;
      }
    }
  }

  fun render : Html {
    <div::test/>
  }
}
--------------------------------------------------------------------------------
class A extends _C {
  $aa() {
    const _ = {};

    (true ? Object.assign(_, {
      [`--a-a`]: `red`
    }) : Object.assign(_, {
      [`--a-a`]: `blue`
    }));

    (true ? Object.assign(_, {
      [`--b-a`]: `yellow`
    }) : Object.assign(_, {
      [`--b-a`]: `cyan`
    }));

    return _;
  }

  render() {
    return _h("div", {
      className: `aa`,
      style: _style([this.$aa()])
    });
  }
};

A.displayName = "Main";

_insertStyles(`
.aa .tag {
  color: var(--a-a);
}

.aa .string {
  color: var(--b-a);
}
`);
