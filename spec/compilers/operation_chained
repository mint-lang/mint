component Main {
  fun render : String {
    "a" == "b" && true != false
    ""
  }
}
--------------------------------------------------------------------------------
import { compare as B } from "./runtime.js";

export const A = () => {
  B(`a`, `b`) && !B(true, false);
  return ``
};
