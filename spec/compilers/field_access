type X {
  name : String
}

module Array {
  fun map (array : Array(a), method : Function(a, b)) : Array(b) {
    ``
  }
}

component Main {
  fun render : String {
    [
      {
        name: "Joe"
      },
      {
        name: "Doe"
      }
    ]
    |> Array.map(.name(X))

    "asd"
  }
}
--------------------------------------------------------------------------------
import {
  access as B,
  record as A
} from "./runtime.js";

export const
  a = (b, c) => {
    return undefined
  },
  d = A(`X`),
  C = () => {
    a([
      d({
        name: `Joe`
      }),
      d({
        name: `Doe`
      })
    ], B(`name`));
    return `asd`
  };
