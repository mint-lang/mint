component Main {
  fun test : Bool {
    "a" == "b" && true != false
  }

  fun render : String {
    try {
      test()

      ""
    }
  }
}
--------------------------------------------------------------------------------
class $Main extends Component {
  test() {
    return _compare(`a`, `b`) && !_compare(true, false)
  }

  render() {
    return (() => {
      this.test()

      return ``
    })()
  }
}

$Main.displayName = "Main"
