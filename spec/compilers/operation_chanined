component Main {
  test : Bool {
    "a" == "b" && true != false
  }

  render : String {
    try {
      test()

      ""
    }
  }
}
--------------------------------------------------------------------------------
class A extends _C {
  a() {
    return _compare(`a`, `b`) && !_compare(true, false);
  }

  render() {
    return (() => {
      this.a();
      return ``;
    })();
  }
};

A.displayName = "Main";
