type Maybe(value) {
  Just(value)
  Nothing
}

component Main {
  fun render : Html {
    <div as input>
    </div>
  }
}
--------------------------------------------------------------------------------
import {
  createElement as C,
  useRefSignal as B,
  variant as A,
  setRef as D
} from "./runtime.js";

export const
  E = A(1, `Just`),
  F = A(0, `Nothing`),
  G = () => {
    const a = B(new F());
    return C(`div`, {
      ref: D(a, E, F)
    })
  };
