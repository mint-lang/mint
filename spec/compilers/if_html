component Main {
  fun render : String {
    if <></> {
      "a"
    } else {
      "b"
    }
  }
}
--------------------------------------------------------------------------------
export const A = () => {
  return (null ? `a` : `b`)
};
