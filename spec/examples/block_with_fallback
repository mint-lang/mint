type Maybe(a) {
  Nothing
  Just(a)
}

component Main {
  state a : Maybe(String) = Just("")

  fun render : Html {
    let Just(_) =
      a

    let Just(_) =
      a

    <span>"Hello"</span>
  } or <span>"Fallback"</span>
}
