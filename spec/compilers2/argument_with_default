component Main {
  fun test (a : String, b : Number = 0) : Number {
    b
  }

  fun render : String {
    test("")

    ""
  }
}
--------------------------------------------------------------------------------
import { define as B } from "runtime";

const A = () => {
  const a = B('a', (b, c = 0) => {
    return c
  });
  a(``);
  return ``
};
