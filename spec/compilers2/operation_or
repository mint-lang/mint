enum Maybe(a) {
  Nothing
  Just(a)
}

component Main {
  fun render : String {
    Maybe::Nothing or "Hello"
    ""
  }
}
--------------------------------------------------------------------------------
import {
  variant as B,
  or as E
} from "runtime";

const
  A = B(1),
  C = B(0),
  D = () => {
    E(new C()._0, `Hello`);
    return ``
  };
