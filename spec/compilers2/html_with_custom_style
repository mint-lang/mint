component Main {
  get styles : Map(String, String) {
    ``
  }

  fun render : Html {
    <div style={styles}>
    </div>
  }
}
--------------------------------------------------------------------------------
import {
  createElement as C,
  useComputed as B,
  style as D
} from "runtime";

const A = () => {
  const a = B(() => {
    return undefined
  });
  return C("div", {
    style: D([a.value])
  })
};
