component Main {
  state color : String = "blue"

  style test {
    @supports (screen) {
      color: #{color};
    }
  }

  fun render : Html {
    <div::test/>
  }
}
--------------------------------------------------------------------------------
import {
  createElement as D,
  useFunction as C,
  useSignal as B,
  style as E
} from "runtime";

const A = () => {
  const
    a = B(`blue`),
    b = C(() => {
      const _ = {
        [`--a-a`]: a.value
      };
      return _
    });
  return D(`div`, {
    className: `Main_test`,
    style: E([b()])
  })
};

@supports (screen) {
  .Main_test {
    color: var(--a-a);
  }
}
