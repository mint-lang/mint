type Result(error, value) {
  Err(error)
  Ok(value)
}

component Main {
  fun render : String {
    decode (`[]`) as Tuple(String, Number, String)

    ""
  }
}
--------------------------------------------------------------------------------
import {
  decodeNumber as G,
  decodeString as F,
  decodeTuple as E,
  variant as B
} from "./runtime.js";

export const
  A = B(1),
  C = B(1),
  D = () => {
    E([
      F(C, A),
      G(C, A),
      F(C, A)
    ], C, A)((([])));
    return ``
  };
