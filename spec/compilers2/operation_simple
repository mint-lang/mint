component Main {
  fun render : String {
    "a" == "b"
    ""
  }
}
--------------------------------------------------------------------------------
import { compare as B } from "runtime";

const A = () => {
  B(`a`, `b`);
  return ``
};
