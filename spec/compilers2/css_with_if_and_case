component Main {
  style test {
    color: yellow;

    case (true) {
      true => color: yellow;
      => color: cyan;
    }

    if (true) {
      color: red;
    } else {
      color: blue;
    }
  }

  fun render : Html {
    <div::test/>
  }
}
--------------------------------------------------------------------------------
import {
  createElement as D,
  useFunction as B,
  style as E,
  match as C
} from "runtime";

const A = () => {
  const a = B(() => {
    const _ = {};
    C(true, [
      [
        true,
        () => {
          return Object.assign(_, {
            [`--a-a`]: `yellow`
          })
        }
      ],
      [
        null,
        () => {
          return Object.assign(_, {
            [`--a-a`]: `cyan`
          })
        }
      ]
    ]);
    (true ? Object.assign(_, {
      [`--a-a`]: `red`
    }) : Object.assign(_, {
      [`--a-a`]: `blue`
    }));
    return _
  });
  return D(`div`, {
    className: `Main_test`,
    style: E([a()])
  })
};

.Main_test {
  color: var(--a-a, yellow);
}
