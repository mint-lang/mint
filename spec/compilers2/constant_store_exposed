store Test {
  const NAME = "ASD"
}

component Main {
  connect Test exposing { NAME }

  fun render : Html {
    <div>
      <{ NAME }>
    </div>
  }
}
--------------------------------------------------------------------------------
import { createElement as B } from "runtime";

const a = `ASD`;

const A = () => {
  return B("div", {}, [a])
};
