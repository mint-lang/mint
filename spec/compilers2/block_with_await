component Main {
  fun promise : Promise(String) {
    `` as Promise(String)
  }

  fun promiseTest : Promise(String) {
    await promise()
  }

  fun render : String {
    promiseTest()

    "Hello"
  }
}
--------------------------------------------------------------------------------
import { useFunction as B } from "runtime";

const A = () => {
  const
    a = B(() => {
      return undefined
    }),
    b = B(async () => {
      return await a()
    });
  b();
  return `Hello`
};
