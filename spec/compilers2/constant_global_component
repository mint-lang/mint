global component Test {
  const NAME = "ASD"

  fun render : String {
    NAME
  }
}

component Main {
  fun render : Html {
    <div>
      <{ Test:NAME }>
    </div>
  }
}
--------------------------------------------------------------------------------
import { createElement as B } from "runtime";

const A = () => {
  return B("div", {}, [a])
};

const a = `ASD`;
const C = () => {
  return a
};
