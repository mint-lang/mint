module Test {
  const NAME = "ASD"
}

component Main {
  fun render : Html {
    <div>
      <{ Test:NAME }>
    </div>
  }
}
--------------------------------------------------------------------------------
import { createElement as B } from "runtime";

export const
  a = `ASD`,
  A = () => {
    return B(`div`, {}, [a])
  };

