component Main {
  fun render : String {
    "a" == "b" && true != false
    ""
  }
}
--------------------------------------------------------------------------------
import { compare as B } from "runtime";

const A = () => {
  B(`a`, `b`) && !B(true, false);
  return ``
};
