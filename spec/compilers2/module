module Html.Testing {
  fun renderAll : Html {
    <p>
      <{ "It should work" }>
    </p>
  }
}

component Main {
  fun render : Html {
    Html.Testing.renderAll()
  }
}
--------------------------------------------------------------------------------
import { createElement as B } from "runtime";

const A = () => {
  return a()
};

const a = () => {
  return B("p", {}, [`It should work`])
};
