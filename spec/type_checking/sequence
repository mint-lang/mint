component Main {
  test : Promise(Never, String) {
    sequence {
      "Hello"
    }
  }

  render : Html {
    <div/>
  }
}
--------------------------------------------------------------------------------
module Promise {
  resolve (input : b) : Promise(Never, b) {
    ``
  }
}

component Main {
  test : Promise(Never, String) {
    sequence {
      x =
        Promise.resolve("Hello")

      x + " World!"
    }
  }

  render : Html {
    <div/>
  }
}
