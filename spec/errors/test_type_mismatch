suite "X" {
  test "X" {
    "true"
  }
}
--------------------------------------------------------------------------------
░ ERROR (TEST_TYPE_MISMATCH) ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░

The type of a test does not match any of the allowed types.

I was expecting one of:

  Promise(Test.Context(a))
  Test.Context(a)
  Promise(Bool)
  Bool

Instead it is:

  String

The test in question is here:

   ┌ errors/test_type_mismatch:3:5
   ├──────────────────────────────
  1│ suite "X" {
  2│   test "X" {
  3│     "true"
   │     ⌃⌃⌃⌃⌃⌃
  4│   }
  5│ }
