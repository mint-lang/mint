name: CI Nightly

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/ci-base.yml
    with:
      crystal-version: nightly

  ameba:
    uses: ./.github/workflows/ameba.yml
