<article>
  <h2>
    Type Error
  </h2>

  <p>
    The return type of the <b>{{index}}. branch</b> of a case expression does
    not match the type of the first branch.
  </p>

  <p>
    Expected <code>{{expected}}</code>, got <code>{{got}}</code>.
  </p>

  {{node}}
</article>
