message PropertyTypeOrDefaultNeeded do
  title "Type Error"

  block do
    text "The"
    bold "type"
    text "or"
    bold "default value"
    text "of a property needs to be defined, but neither was."
  end

  snippet node
end
