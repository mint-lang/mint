<article>
  <h2>
    Type Error
  </h2>

  <p>
    The type of the {{side}} operand <code>{{value}}</code> does not match the
    type of the operation <code>+</code>
  </p>

  <p>
    Expected <code>Number</code> or <code>String</code>, got
    <code>{{value}}</code>.
  </p>

  {{node}}
</article>
