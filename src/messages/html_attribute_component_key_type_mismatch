<article>
  <h2>
    Type Error
  </h2>

  <p>
    The key attribute of a component has an invalid type. It can only be
    <code>String</code>.
  </p>

  <p>
    Expected <code>{{expected}}</code> but got <code>{{got}}</code> instead.
  </p>

  {{node}}
</article>
