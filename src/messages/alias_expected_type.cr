message AliasExpectedType do
  title "Syntax Error"

  block do
    text "I was looking for a"
    bold "type of an alias"
    text "but found"
    char got
    text "instead."
  end

  snippet node
end
