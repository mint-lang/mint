message ProviderNotFoundSubscription do
  title "Type Error"

  block do
    text "I was looking for the record type of the subscription"
    bold name
    text "but could not find it."
  end

  snippet node
end
