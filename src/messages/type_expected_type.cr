message TypeExpectedType do
  title "Syntax Error"

  block do
    text "I was looking for an"
    bold "uppercase letter for a type"
    text "but found"
    char got
    text "instead."
  end

  snippet node
end
