<article>
  <h2>
    Type Error
  </h2>

  <p>
    The <b>falsy branch of an if expression</b> does not match the type of the
    thruty branch.
  </p>

  <p>
    I was expecting <code>{{expected}}</code> but found <code>{{got}}</code>
    instead.
  </p>

  {{node}}
</article>
