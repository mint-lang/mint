<article>
  <h2>
    Type Error
  </h2>

  <p>
    A child node of an element or component has an invalid type.
  </p>

  <p>
    I was expecting one of the following types: <code>Html</code>
    <code>String</code> <code>Array(Html)</code> <code>Array(String)</code> but
    found <code>{{got}}</code> instead.
  </p>

  {{node}}
</article>
