message PropertyNotHole do
  title "Type Error"

  block do
    text "A properties type contains type variables."
  end

  block do
    text "Type variables in properties are not allow at this time."
  end

  snippet node
end
