<article>
  <h2>
    Type Error
  </h2>

  <p>
    The type signature of the function <code>{{name}}</code> of a component must
    be <code>Function(Void)</code>.
  </p>

  <p>
    Expected <code>{{expected}}</code>, got <code>{{got}}</code>.
  </p>

  {{node}}
</article>
