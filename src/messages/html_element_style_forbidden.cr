message HtmlElementStyleForbidden do
  title "Type Error"

  block do
    text "Inline styles on elements are forbidden."
  end

  block do
    text "If you want to style an element, use a component with styles."
  end

  snippet node
end
