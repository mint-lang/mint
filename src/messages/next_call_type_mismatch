<article>
  <h2>
    Type Error
  </h2>

  <p>
    A <b>next</b> can only set the state of <b>a component or a store</b>
    to a record that matches their definition.
  </p>

  <p>
    Expected <code>{{expected}}</code>, got <code>{{got}}</code>.
  </p>

  {{node}}
</article>
