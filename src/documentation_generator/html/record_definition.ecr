<h1>
  <%= stringify(node) %>
  <div class="type-badge" data-type="record"></div>
  <a class="github-source-link" href="<%= git_url(node) %>"></a>
</h1>

<h2>Fields</h2>

<% node.fields.sort_by(&.key.value).each do |f| %>
  <%= generate(f) %>
<% end %>